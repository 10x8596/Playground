import React from 'react';

const Card = ({ title }) => {
  return (
    <div className="card">
      <h3>{title}</h3>
      {/* Add other content for the card */}
    </div>
  );
};

export default Card;
